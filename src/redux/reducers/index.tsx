import { combineReducers } from "redux";
import Reducer from "./reducer";

export default combineReducers({
    Reducer,
});

// başka reducer olursa hepsini birleştirmek için combineReducers kullanıldı
