import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        height: 55,
        width: "100%",
        borderRadius: 30,
        borderWidth: 1,
        backgroundColor: "#000",
        marginTop: 100,
    },
    buttonText: {
        color: "#FFF",
        textAlign: "center",
        fontSize: 18,
        fontWeight: "900",
        alignItems: "center",
        paddingVertical: 15,
        paddingHorizontal: 10,
    },
});
